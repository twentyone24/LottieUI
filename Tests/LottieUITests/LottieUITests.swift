import XCTest
@testable import LottieUI

final class LottieUITests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(LottieUI().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
